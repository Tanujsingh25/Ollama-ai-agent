# Ollama-ai-agent
AI Agent to interact with K8s via CLI

Basic dir structure

k8s-ai-agent/
│
├── agent.py          # main CLI loop
├── llm.py            # GPT-4 interaction
├── executor.py       # Kubernetes actions
├── safety.py         # action validation
├── .env
└── requirements.txt